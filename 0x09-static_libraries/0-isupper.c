#include "main.h"
/**
 * _isupper - detect uppercase alphabets
 * @c : input character
 * Return: True for (1) and false for (0)
 *
 */
int _isupper(int c)

{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
