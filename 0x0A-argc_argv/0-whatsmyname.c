#include <stdio.h>
#include "main.h"
/**
 * main - prints its name, followed by a new line
 * @argc: number of command line argument
 * @argv: array containg the command line argument
 *
 * Return: 0
 */
int main(int argc __attribute__((unused)), char *argv[])

	printf("%s\n", *argv);
	return (0);
